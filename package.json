{
  "name": "if-file-exists",
  "version": "1.0.0",
  "description": "Verify if file exists in the server",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Diego Augusto da Silva",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.0"
  }
}
